sudo: required
dist: trusty

language: php
php:
   - '7.0'

addons:
   chrome: stable

install:
   - php -v
   - cp .env.dusk .env
   - travis_retry composer install --no-interaction --prefer-dist --no-suggest

before_script:
   - google-chrome-stable --headless --disable-gpu --remote-debugging-port=9515 http://beta-sm.45.in &

script:
   - php artisan dusk tests/Browser/LoginTest.php
